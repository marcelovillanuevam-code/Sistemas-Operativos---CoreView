// ready-queue.js — DOM renderer for the ready queue. Renders SchedulableEntity cards with CSS transitions.

/**
 * @param {HTMLElement} container
 * @param {import('../types.js').TimelineEntry} entry
 */
export function renderReadyQueue(container, entry) {
  throw new Error('Not implemented');
}
