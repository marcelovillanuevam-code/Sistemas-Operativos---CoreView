// scheduling-priority.js — Preemptive Priority scheduling algorithm. Pure function, zero DOM.
// Lower priority number = higher priority. Calls expandToThreads() internally. Returns SchedulingTrace.

/** @param {import('../types.js').Process[]} processes @returns {import('../types.js').SchedulingTrace} */
export function runPriorityPreemptive(processes) {
  throw new Error('Not implemented');
}
