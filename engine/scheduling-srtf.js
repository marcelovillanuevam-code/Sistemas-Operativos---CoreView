// scheduling-srtf.js — Shortest Remaining Time First (preemptive SJF) algorithm. Pure function, zero DOM.
// Calls expandToThreads() internally. Returns SchedulingTrace.

/** @param {import('../types.js').Process[]} processes @returns {import('../types.js').SchedulingTrace} */
export function runSRTF(processes) {
  throw new Error('Not implemented');
}
