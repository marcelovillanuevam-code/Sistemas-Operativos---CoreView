// paging-optimal.js — Optimal (Bélády's) page replacement algorithm. Pure function, zero DOM.
// Evicts the page whose next use is furthest in the future. Returns PageReplacementTrace.

/** @param {number} numFrames @param {import('../types.js').PageRef[]} refs @returns {import('../types.js').PageReplacementTrace} */
export function runOptimal(numFrames, refs) {
  throw new Error('Not implemented');
}
