// scheduling-hrrn.js — Highest Response Ratio Next scheduling algorithm. Pure function, zero DOM.
// Calls expandToThreads() internally. RR = (waitTime + burstTime) / burstTime. Returns SchedulingTrace.

/** @param {import('../types.js').Process[]} processes @returns {import('../types.js').SchedulingTrace} */
export function runHRRN(processes) {
  throw new Error('Not implemented');
}
